name: CI
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: timayz/.github/.github/workflows/service-testing.yml@v2.18.0
    with:
      db_name: starter

  release:
    needs: [test]
    if: always() && !cancelled() && !contains(needs.*.result, 'failure')
    uses: timayz/.github/.github/workflows/service-release.yml@v2.18.0
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
